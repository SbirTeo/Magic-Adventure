# ==========================================================================================
#  cornice-test.yml — banco di prova per la cornice a glifo font (vedi CLAUDE.md,
#  "Copiare un file BINARIO nuovo sul VPS" e MagixPack/overrides-vps/).
#
#  TEST DIAGNOSTICO: il titolo contiene il glifo del LOGO DEL TABLIST (), quello che
#  gia' funziona li'. Se compare anche qui, il problema e' solo l'immagine/i valori della
#  cornice. Se NON compare nemmeno questo, il titolo dei bauli non risolve i glifi custom del
#  font in questa versione — serve un'altra strada (es. un item invece di un glifo nel titolo).
#
#  NESSUN item negli slot: il client disegna le icone degli item SOPRA il testo del titolo,
#  quindi coprirebbero il glifo.
# ==========================================================================================

menu:
  type: chest
  rows: 6
  title: ""
  update: 0
  commands: [cornicetest]
  closeable: true

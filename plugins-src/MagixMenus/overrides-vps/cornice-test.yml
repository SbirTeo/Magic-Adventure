# ==========================================================================================
#  cornice-test.yml — banco di prova per la cornice a glifo font (vedi CLAUDE.md,
#  "Copiare un file BINARIO nuovo sul VPS" e MagixPack/overrides-vps/).
#
#  CAUSA TROVATA (con fonte): ogni glifo bitmap di Minecraft ha un limite FISSO di 256x256
#  pixel nativi (non il campo "height" del JSON, che riscala solo la resa a schermo — e'
#  proprio la risoluzione del file). crystal_frame.png era 976x1000: ridotto a 250x256,
#  dentro il limite. Titolo: solo il glifo (), senza spostamento, per vedere posizione
#  e dimensione prima di ricalibrare.
#
#  NESSUN item negli slot: il client disegna le icone degli item SOPRA il testo del titolo,
#  quindi coprirebbero il glifo.
# ==========================================================================================

menu:
  type: chest
  rows: 6
  title: ""
  update: 0
  commands: [cornicetest]
  closeable: true

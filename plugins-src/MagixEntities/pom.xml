<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.teolo</groupId>
    <artifactId>MagixEntities</artifactId>
    <version>0.9.13</version>
    <packaging>jar</packaging>

    <name>MagixEntities</name>
    <description>NPC/entita' statiche create da comando (stile Citizens), con skin giocatore via Mannequin</description>

    <properties>
        <!-- paper-api 26.1.2 e' compilata per Java 25 (class file 69.0): serve JDK 25 per buildare.
             Sul PC di sviluppo c'e' in plugins-src/MagixFactions/jdk-25.0.3+9 (vedi README). -->
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>papermc-repo</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <!-- Stesso build del server (log: "Paper version 26.1.2-72-ver/26.1.2"). -->
            <version>26.1.2.build.72-stable</version>
            <scope>provided</scope>
        </dependency>
        <!-- Solo per compilare contro MagixLanguageAPI (softdepend, mai imbustato nel jar): il
             workflow di deploy installa MagixLanguage nel repo Maven locale prima di questo
             plugin (vedi .github/workflows/deploy-plugin.yml). A runtime, se MagixLanguage non
             e' installato sul server, il codice non tocca mai questa classe (vedi lang/Messages). -->
        <dependency>
            <groupId>com.teolo</groupId>
            <artifactId>MagixLanguage</artifactId>
            <version>0.4.4</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.name}-${project.version}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
            <!-- README nel jar: il plugin lo riscrive in plugins/MagixEntities/ ad ogni avvio. -->
            <resource>
                <directory>${project.basedir}</directory>
                <includes>
                    <include>README.md</include>
                </includes>
                <filtering>false</filtering>
            </resource>
        </resources>
    </build>
</project>
